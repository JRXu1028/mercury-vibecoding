# Overleaf Usage Notes

1. Create a new Overleaf project.
2. Upload `main.tex` from this folder.
3. Set compiler to `XeLaTeX`.
4. Replace image placeholders in slides with real screenshots if needed.

Suggested screenshots to replace placeholders:
- Desktop main UI with 3-pane layout
- Feed add / sync result
- OPML import dialog or result
