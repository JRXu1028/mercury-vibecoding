# Vibe Reader Team A 协作纪实

> 一段 A 组（李欣昊、周孙睿、朱宇瑄）围绕 Feed、OPML、同步与阅读入口完成项目最小闭环的协作记录。
> 从项目 MVP 定义到桌面端阅读器 UI 收敛，记录 Team A 如何把“输入 RSS URL”变成一个可实际使用的本地订阅阅读器。

---

## 序章 · 从订阅源开始

Vibe Reader 的核心不是 AI，也不是复杂的阅读样式，而是一个最朴素的入口：

> 输入一个 RSS / Atom 地址，应用能保存订阅源，拉取文章，并在本地展示文章列表。

这就是 Team A 的任务边界。

A 组负责 Feed 添加 / 删除、RSS / Atom 解析、OPML 导入导出、Feed 同步和文章列表查询。换句话说，其他组后续的正文清洗、Reader 阅读视图、AI 摘要和翻译，都必须建立在 A 组稳定产出的 `feeds` 与 `entries` 数据之上。

---

## 第一章 · MVP 闭环：Feed 入库与文章列表

项目初期，README 里对 Team A 的验收标准非常直接：

> 输入 RSS URL -> 保存订阅源 -> 显示文章标题列表。

围绕这个闭环，A 组先落地了两张基础表：

- `feeds`
- `entries`

其中 `entries` 使用 `UNIQUE(feed_id, guid)` 约束，保证重复同步时不会反复插入同一篇文章。

这一阶段的核心取舍是：数据库使用 SQLite，本地优先，不依赖远端账号体系；Feed 解析使用 `rss-parser`，OPML 解析与构建使用 `fast-xml-parser`。这样既满足课程项目的可运行性，也保留了未来扩展成本较低的结构。

最小命令行能力也同步完成：

```bash
add <url>
list-feeds
list-entries [feedId]
sync <feedId>
sync-all
remove <feedId>
import-opml <file>
export-opml <file>
```

到这里，项目已经具备了 RSS 阅读器最基本的数据流。

---

## 第二章 · OPML 与同步：从单源测试到批量订阅

只支持单个 Feed URL 不够像真实 Reader。A 组继续补齐 OPML：

- 导入 OPML：批量解析订阅源 URL
- 导出 OPML：把当前订阅源导出为标准文件
- 失败项记录：导入失败时保留 URL 与原因

这一阶段的难点不是 UI，而是容错：

- 有些中文网站 Feed 返回慢
- 有些源会跳转
- 有些条目缺少标准字段
- 有些文章时间、作者、摘要字段不稳定

因此 FeedService 的实现重点放在“能持续同步、能去重、失败不拖垮整体流程”。同步能力也逐步形成三种入口：

- 同步当前 Feed
- 同步所有 Feed
- 定时自动同步

这让项目从“能解析一个测试源”变成了“能作为日常订阅工具使用”。

---

## 第三章 · Electron 桌面化与三栏入口

CLI 闭环跑通后，A 组把功能接入 Electron + Vue 前端。

最早的 UI 采用三栏结构：

- 左侧：Feed 列表与订阅源操作
- 中间：文章列表与搜索
- 右侧：文章详情占位，后续接入 Reader

这个布局不是最初文档里的硬性要求，但它非常适合 RSS 阅读器的信息流：

1. 先选择订阅源。
2. 再浏览文章列表。
3. 最后进入正文阅读。

同时，A 组通过 preload / IPC 把本地后端能力暴露给 Renderer：

- 添加 Feed
- 删除 Feed
- 同步 Feed
- 查询文章列表
- 导入 / 导出 OPML

这一阶段之后，Team B 的正文清洗和 Reader 阅读视图可以直接挂载在右侧详情区域，Team C 的 AI 摘要与翻译也有了稳定的文章入口。

---

## 第四章 · 真实 Feed 测试与中文站点验证

为了避免只在 mock 数据里自测，A 组使用真实 Feed 做过导入和同步验证，包括：

- 阮一峰的网络日志
- 美团技术团队
- Hacker News Front Page
- V2EX

测试暴露出一个重要事实：真实 Feed 的质量不稳定。

例如有些源可以正常返回 XML，但在项目 parser 里可能超时；有些文章摘要和正文结构并不一致；有些中文站点的时间格式、作者字段也不是完全规范。

这些测试让 A 组明确了后续设计原则：

- Feed 导入要允许部分失败
- 同步结果要能反馈新增文章数量
- 文章列表不能假设所有字段都存在
- UI 上要给用户保留刷新和重新同步入口

---

## 第五章 · v0.2.5：侧栏折叠与空间回收

随着 B/C/D 组功能不断接入，界面上方和三栏区域逐渐变得拥挤。

2026-06-24，A 组完成了一次体验修复：

- 侧栏折叠时文字与图标不再溢出
- 中间文章列表折叠态同步适配
- 删除冗余顶部 Header 栏
- 回收垂直空间，让正文更早出现

这次改动看起来不大，但对桌面阅读器很关键：默认界面应该优先服务阅读，而不是长期展示项目标题或重复工具区。

---

## 第六章 · Reader 风格 UI 收敛

后续评审中，团队发现一个问题：

> 功能已经足够多，但默认界面仍然有“管理后台感”，不像一个真正的 Reader。

因此 A 组继续推动 UI 做减法，参考 Reeder、Readwise Reader、NetNewsWire 等主流阅读软件，把界面重新收敛为更安静的阅读体验。

主要调整包括：

- 右侧顶部工具栏压缩为标题、元信息、阅读工具三行
- Provider 不再直接暴露在主界面，只放入 AI 菜单
- AI 功能作为增强能力出现，而不是长期占据阅读区上方
- OPML 导入导出移入左侧更多菜单
- Auto Sync 移到底部设置区
- Feed 删除按钮改为 hover 显示
- Feed 数量右对齐
- 文章列表标题最多两行
- 时间改为“今天 16:29 / 昨天 10:30 / 3 天前”等相对展示
- Notes 默认收起，改为右下角按钮和右侧抽屉

这次重构的目标不是增加新功能，而是统一视觉语义：

- 主色：暖棕 / 琥珀，用于选中和主要状态
- AI 色：橙色，用于 AI 功能
- 危险色：红色，只用于删除与确认类操作
- 中性色：灰色，用于边框、辅助文字、未激活控件

---

## 第七章 · 当前结果

截至当前版本，Team A 已完成：

- Feed 添加 / 删除
- RSS / Atom 解析
- Feed 单源同步与全量同步
- 定时自动同步
- OPML 导入 / 导出
- 文章列表查询与搜索
- Electron 桌面端 Feed / Entry 入口
- 三栏 Reader 信息架构
- 侧栏折叠适配
- Reader 风格 UI 收敛

验证结果：

```bash
npm run build:desktop
npm test
```

当前测试结果为 15 个测试文件、54 个测试全部通过。

---

## 尾声 · Team A 的边界

Team A 的工作最终证明了一件事：

> 一个阅读器最重要的第一步，是稳定地把信息带进来。

只有 Feed、OPML、同步和文章列表可靠，后续的正文清洗、Markdown 渲染、AI 摘要、AI 翻译和笔记标签才有意义。

A 组完成的是 Vibe Reader 的入口，也是整个应用的数据地基。
